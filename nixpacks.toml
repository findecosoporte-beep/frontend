[phases.setup]
nixPkgs = ["nodejs_22"]

# Las dependencias se instalan en scripts/build.sh (instalación limpia por plataforma).
[phases.install]
cmds = ["echo 'deps en build.sh'"]

[variables]
NPM_CONFIG_CACHE = "/tmp/npm-cache"
